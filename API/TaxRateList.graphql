query TaxRateList {
  taxRateList {
    items {
      code
      rate
      region_name
      tax_calculation_rate_id
      tax_country_id
      tax_postcode
      tax_region_id
      titles
    }
    total_count
  }
}
